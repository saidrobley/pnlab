export default function StatsBar() {
  return (
    <div className="px-10 py-10 border-t border-b border-border">
      <p className="text-center text-sm text-text-muted font-light">
        Currently in private beta — join the waitlist to get early access
      </p>
    </div>
  );
}
